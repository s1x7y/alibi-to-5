# alibi-to-5 dependencies
#
# Install everything with:   brew bundle
# (run from this folder, the one containing this Brewfile)

brew "cliclick"   # scriptable mouse mover; keeps Slack/Teams from showing "away"

# Optional: the Codex CLI. The wake routine pings it to start its usage window,
# but it is not required -- if absent, that step is skipped with a logged
# warning. Install separately if you want it (see the Codex CLI docs); it is not
# a Homebrew formula, so it is intentionally not listed here.
